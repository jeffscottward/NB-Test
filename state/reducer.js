// import globalValues from "../constants/globalValues";

export default function reducer(state, action) {
  switch (action.type) {
    case "CASE_1":
      return { ...state };
    case "CASE_2":
      return { ...state };
    default:
      return { ...state };
  }
}
